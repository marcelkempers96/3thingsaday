import { Baloo_2, Nunito, Inter } from 'next/font/google';

export const fontBaloo = Baloo_2({ subsets: ['latin'], weight: ['400', '600', '700'], display: 'swap' });
export const fontNunito = Nunito({ subsets: ['latin'], weight: ['400', '600', '700'], display: 'swap' });
export const fontInter = Inter({ subsets: ['latin'], weight: ['400', '700'], display: 'swap' });